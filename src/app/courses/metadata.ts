import { generateMetadata } from '@/utils/openGraphUtils';

export const metadata = generateMetadata(
  'about',
  'אודות | The Fader מספרה בטירת הכרמל',
  'הכירו את הצוות המקצועי שלנו וגלו את הסיפור מאחורי המספרה המודרנית המובילה בטירת הכרמל.'
);